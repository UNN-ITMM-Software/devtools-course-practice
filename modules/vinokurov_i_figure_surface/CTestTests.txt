#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

# Can Run the application
add_test(
    NAME ${prefix}_can_Run
    COMMAND ${APPLICATION}
)
