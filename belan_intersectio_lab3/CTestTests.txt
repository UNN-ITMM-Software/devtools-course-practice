#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

add_test(
    NAME ${prefix}_intersection
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_intersection PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments intersect"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_no_intersection
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_no_intersection PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments do not intersect"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_parallel
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_parallel PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments are parallel"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_no_parallel
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_no_parallel PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments are not parallel"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_collinear
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_collinear PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments are collinear"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_vertical_segments
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_vertical_segments PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments are vertical"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_horizontal_segments
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_horizontal_segments PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments are horizontal"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_counter_clockwise_orientation
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_counter_clockwise_orientation PROPERTIES
    PASS_REGULAR_EXPRESSION "Counter-clockwise orientation"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_get_x
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_get_x PROPERTIES
    PASS_REGULAR_EXPRESSION "GetX test passed"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_get_y
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_get_y PROPERTIES
    PASS_REGULAR_EXPRESSION "GetY test passed"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_different_orientations
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_different_orientations PROPERTIES
    PASS_REGULAR_EXPRESSION "Different orientations test passed"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_same_orientations
    COMMAND ${TESTS}
)
set_tests_properties (${prefix}_same_orientations PROPERTIES
    PASS_REGULAR_EXPRESSION "Same orientations test passed"
    LABELS "${MODULE}")


add_test(
    NAME ${prefix}_run_application
    COMMAND ${APPLICATION} 1 1 3 3 2 2 4 4
)
set_tests_properties (${prefix}_run_application PROPERTIES
    PASS_REGULAR_EXPRESSION "Segments intersect"
    LABELS "${MODULE}"
)